package fr.gtm.comptebancaire;

public class CompteBancaire {
	public int validationCompte() {
		
		int numBanque;
		int codeEtab;
		int numCompte;
		int rib;
		int longueur;
		//int concat = (numBanque+codeEtab+numCompte+rib);
		
		
		for(numBanque = 0 ; numBanque <= 5 ; numBanque++) {
			return numBanque;			
		}
			char i = (char) (numBanque);
		
		for(codeEtab = 0 ; codeEtab <= 5 ; codeEtab++) {
			return codeEtab;
		}
			char j = (char) (codeEtab);
			
		for(numCompte = 0 ; numCompte <= 11 ; numCompte++) {
			return(numCompte);
		}
			while (longueur == 11) {	//ajouter un zéro si la longueur est égale à zéro.
				return;
			}
			char k = (char) (numCompte);
		
		for(rib = 0 ; rib <= 2 ; rib++) {
			return(rib);
		}
			char l = (char) (rib);
		
			
		
		
					
		
	}
}
